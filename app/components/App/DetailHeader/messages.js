/*
 * DetailHeader Messages
 *
 * This contains all the text for the DetailHeader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DetailHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DetailHeader component!',
  },
});
