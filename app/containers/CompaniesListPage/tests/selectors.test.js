// import { selectCompaniesListPageDomain } from '../selectors';

describe('selectCompaniesListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
