/**
 *
 * PageHeaderTypography
 *
 */

import styled from 'styled-components';
import { PRIMARY_RED } from 'utils/colors';

const PageHeaderTypography = styled.h2`
  color: ${PRIMARY_RED};
  font-weight: 400;
  font-size: 16px;
  margin-bottom: 0;
`;

export default PageHeaderTypography;
