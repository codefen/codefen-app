/**
 *
 * LoginFormError
 *
 */

import styled from 'styled-components';
import { SECONDARY_RED } from 'utils/colors';

const LoginFormError = styled.section`
  display: block;
  color: ${SECONDARY_RED};
`;

export default LoginFormError;
