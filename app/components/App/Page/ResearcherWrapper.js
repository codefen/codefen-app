/**
 *
 * ResearcherWrapper
 *
 */

import styled from 'styled-components';
import { PRIMARY_RED } from 'utils/colors';

const ResearcherWrapper = styled.span`
  color: ${PRIMARY_RED};
`;

export default ResearcherWrapper;
