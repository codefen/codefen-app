/**
 *
 * DetailHeaderWrapper
 *
 */

import styled from 'styled-components';
import { PRIMARY_RED } from 'utils/colors';

const DetailHeaderWrapper = styled.section`
  background-color: ${PRIMARY_RED};
  color: #fff;
  padding: 8px 10px;
  font-size: 17.5px;
  margin: 10px 0 0;
`;

export default DetailHeaderWrapper;
